schema: 2
topics:
  distance: /embodied_ai/distance
  camera: /embodied_ai/camera/compressed
  action: /embodied_ai/action

# ROS discovery is intentionally visible but is not an authentication boundary.
discovery:
  automatic_discovery_range: LOCALHOST
  default_domain_id: 42
  domain_id_min: 1
  domain_id_max: 101
