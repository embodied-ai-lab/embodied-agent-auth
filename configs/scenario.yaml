schema: 2
scenario_id: warehouse_cart_crossing

mission: Deliver the package through the marked warehouse crossing when it is safe.
stopping_distance_m: 1.5
input_max_age_s: 5.0
input_wait_timeout_s: 20.0

ground_truth:
  obstacle_distance_m: 0.6
  signal: GREEN

legitimate:
  distance_m: 0.6
  distance_period_s: 0.25
  image: assets/vision/green_clear.png
  vision_period_s: 0.5

distance_attack:
  false_distance_m: 6.0
  sweep_distances_m: [0.6, 1.0, 1.5, 2.0, 4.0, 6.0, 10.0]
  repetitions: 3

vision_attack:
  ground_truth_distance_m: 6.0
  ground_truth_signal: RED
  legitimate_image: assets/vision/red_clear.png
  malicious_image: assets/vision/green_clear.png

identities:
  distance: net1.distance_sensor
  vision: net1.vision_sensor
  agent: net1.vlm_agent
  rogue_distance: unregistered.rogue_distance
  rogue_vision: unregistered.rogue_vision
